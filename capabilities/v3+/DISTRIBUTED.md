=> shared ontology, but far physically located (US/EU/ASIA)
- reads could be replicated (but stale data possible)
- mutations should still happen to be fast and propagate globally?
- how to ensure consistency
- how to model that in the ontology?
- should it be separated kernels ? use federation?